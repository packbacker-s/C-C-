#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;
struct cut(string a) {
  for(int i = 0; i < a.length() - 1; --i) {
    if (a[i] == '0') {

    }
  }
}
string cut_before_zore(string a) {
  int i = 0;
  for( i = 0; i < a.length(); ++i) {
    if(a[i] == '0') {
      flat = 1;
    }
    else {

    }
  }
}
int main(){
  string a,b;
  while(cin >> a>> b) {
    a = cut_before_zore(a);
    b = cut_before_zore
    for(int i = 0; i < a.length(); ++i) {
      if(a[i] == '.') {
        a = cut(a);
        break;
      }
    }
  }
  return 0;
}

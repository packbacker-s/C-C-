#include <iostream>
#include <algorithm>
#include <cstdilb>
using namespace std;
int n;
struct game {
  char name[10];
  char card[5];
  /* data */
};
int main() {
  while(cin >> n) {
    for(int i = 0; i < n; i++) {
      cin >> game.name >> game.card;
    }

  }
  return 0;
}

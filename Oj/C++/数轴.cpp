#include <iostream>
#include <algorithm>
using namespace std;
const int maxn = 100000;
int n;      //板子数量
int B[maxn][maxn];   //表示板子
struct board {
  int x;   //板左边位置
  int y;   //板右边位置
};

int main() {
  cin >> n;
  int a[2]
  for(int )

  return 0;
}
